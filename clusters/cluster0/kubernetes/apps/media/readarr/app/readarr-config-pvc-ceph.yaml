apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readarr-config-ceph
  namespace: media
  labels:
    app: readarr
spec:
  accessModes:
   - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-block
  
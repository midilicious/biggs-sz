apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readarr-config-ceph
  namespace: media
  labels:
    app: readarr
spec:
  accessModes:
   - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ceph-block
  
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ombi-config-ceph
  namespace: media
  labels:
    app: ombi
spec:
  accessModes:
   - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-block
  
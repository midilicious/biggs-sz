apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-config-ceph
  namespace: media
  labels:
    app: plex
spec:
  accessModes:
   - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: ceph-block
  